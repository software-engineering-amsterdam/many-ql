- Add Boolean, Set
- Type checking
- Unique ID nodes in AST

The type checker detects:
    operands of invalid type to operators (Ask how)
    cyclic dependencies between questions (Ask how)
    duplicate labels (warning)