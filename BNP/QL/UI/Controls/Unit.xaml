<UserControl x:Class="QL.UI.Controls.Unit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:QL.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="600"
             Margin="5" Padding="10"
             BorderBrush="DarkBlue" BorderThickness="2px">
    
    <UserControl.Resources>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" FontWeight="Bold">Question text</TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="1" TextAlignment="Right" FontSize="10px">Identifier</TextBlock>
        <controls:TextWidget Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"></controls:TextWidget>
    </Grid>
</UserControl>
